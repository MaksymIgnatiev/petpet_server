# Svelte frontend for PetPet server

1. Install dependencies:
```sh
bun i
```
2. Run in dev mode (not all issues might be fixed)
```sh
bun run dev
```
2. Run `build` script to generate production code
```sh
bun run build
```
