{
  "name": "petpet_http_server",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch src/index.ts",
	"": "Running src/help.ts directly does not invoke all processing stuff => less CPU usage :)",
    "help": "bun run src/help.ts -f"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.3.3"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "gifwrap": "^0.10.1",
    "hono": "^4.6.1",
    "sharp": "^0.33.5",
    "toml": "^3.0.0",
    "zlib": "^1.0.5"
  }
}
